'use strict';

require('should');
var pathify = require('../pathify');

/* global describe it */

describe('pathify general tests', function() {
  it('Example it', function() {
    (true).should.equal(true);
  });
});
